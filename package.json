{
  "name": "dozo",
  "version": "0.0.0",
  "private": true,
  "description": "DOZO monorepo root tooling: commit conventions.",
  "scripts": {
    "commitlint": "commitlint --edit",
    "commitlint:range": "commitlint --from origin/main --to HEAD"
  },
  "devDependencies": {
    "@commitlint/cli": "^21.2.2",
    "@commitlint/config-conventional": "^21.2.2"
  }
}
